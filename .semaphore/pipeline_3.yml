version: v1.0
name: Docker Build
blocks:
  - name: Build
    task:
      jobs:
        - name: Build and deploy
          commands:
            - echo "job 1"
            - cd frontend/xlanche
            - ng build --prod
      prologue:
        commands:
          - checkout
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
