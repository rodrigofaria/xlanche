version: v1.0
name: Docker Build
blocks:
  - name: Build
    task:
      jobs:
        - name: Build and deploy
          commands:
            - cd frontend/xlanche
            - ng build --prod
      prologue:
        commands:
          - checkout
          - nvm install 10.10.0
          - npm install -g @angular/cli
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
